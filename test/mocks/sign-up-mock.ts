export const mockLogger = {
  log: jest.fn(),
  error: jest.fn(),
  warn: jest.fn(),
  debug: jest.fn(),
};

export const mockData = {
  email: 'test@example.com',
  password: 'plainpass',
  user_name: 'testuser',
};
